 <nav class="navbar navbar-expand-md  fixed-top" style="background-color:#fd8c04;">
        <a class="navbar-brand" href="#">DREAMERS</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav mr-auto">
            <li class="navbar-item active">
              <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="navbar-item">
              <a class="nav-link disable" href="#">カテゴリー</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">叶った夢</a>
            </li>
          </ul>
          <% if user_signed_in? %>
          <div class="mr-5 align-items-center" >
          <p><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "btn btn-outline-primary"%></p>
          </div>
          <% else %>
          <div class="mr-5 align-items-center" > 
          <p><%= link_to 'ログイン', new_user_session_path, class: "btn-login btn-lg" %></p>  
           </div>
          <% end %>
          <form class="form-inline mt-2 mt-md-0">
            <input class="form-control" type="text" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
          </form>
        </div>
      </nav>
        

          


